import 'package:flutter/material.dart';

class AfterVotePage extends StatelessWidget {
  const AfterVotePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
